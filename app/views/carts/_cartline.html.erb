<%= render partial: 'shared/copy', locals: {collection: cartline.collection } %>
<%= button_to "Retirer l'album du panier", cartline_destroy_path(cartline.id), method: :delete, class: 'btn btn-danger' %>

<%= button_to "Ajouter à ma wishlist", add_wishlist_path(:release_id => @release.id, :image_url => @release.images[0].uri, :title => @release.title, :artist => @release.artists[0].name, :year => @release.year), class: 'btn btn-cart bi bi-bookmark-star' %>
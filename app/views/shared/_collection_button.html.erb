<%= button_to "  Ajouter à ma collection", add_collection_path(:release_id => @release.id, :image_url => @release.images[0].uri, :title => @release.title, :artist => @release.artists[0].name, :year => @release.year), class: 'bi bi-vinyl btn btn-cart' %>